package actions_test

import (
	"testing"
)

func TestActions(t *testing.T) {
	t.Run("GetRange", func(t *testing.T) {
		// create mocked timeseries and create GetRange function
		// getRange := actions.NewGetRange(nil)
		// getRange(time.Now(), time.Now())
	})
}
